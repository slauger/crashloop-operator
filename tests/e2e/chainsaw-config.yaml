apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Configuration
metadata:
  name: crashloop-operator
spec:
  parallel: 1
  timeouts:
    apply: 1m
    assert: 5m
    cleanup: 2m
    delete: 2m
    error: 1m
